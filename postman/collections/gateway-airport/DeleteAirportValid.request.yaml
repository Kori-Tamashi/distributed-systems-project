# Gateway - Airport Delete Valid
name: Delete Airport Valid
event:
  - listen: test
    script:
      exec:
        - pm.test("Status code is 204 or 500", function () {
            pm.expect(pm.response.code).to.be.oneOf([204, 500]);
          });
