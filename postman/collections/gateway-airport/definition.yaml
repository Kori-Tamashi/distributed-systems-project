info:
  name: Gateway - Airport
  description: API tests for Gateway Airport endpoints
  schema: https://schema.getpostman.com/json/collection/v2.1.0/collection.json

item:
  - name: Health Check
    request:
      method: GET
      header: []
      url:
        raw: "{{gateway_base_url}}/health"
        host:
          - "{{gateway_base_url}}"
        path:
          - health
