$kind: http-request
order: 16000
method: DELETE
url: '{{baseUrl}}/api/v1/persons/99999'
description: |
  ## Delete Person (Not Found)
  
  **Метод:** DELETE  
  **Endpoint:** `/api/v1/persons/{id}`  
  **Ожидаемый статус:** 404 Not Found  
  
  ### Описание
  Проверяет обработку случая, когда попытка удалить несуществующего Person.  
  Используется несуществующий ID (99999).
  
  ### Path Parameters
  - `id` - несуществующий ID (99999)
  
  ### Ожидаемый результат
  - Статус: 404 Not Found
  - Person не найден в БД
  
  ### Автор
  Lab 01 - CI/CD Pipeline (2026-09-10)
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Status is 404 Not Found', function () {
          pm.response.to.have.status(404);
      });
