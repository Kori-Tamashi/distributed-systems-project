$kind: http-request
order: 1200
method: DELETE
url: '{{baseUrl}}/api/v1/privileges/3'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |
      pm.test('Status is 204, 404, or 500', function () {
        pm.expect(pm.response.code).to.be.oneOf([204, 404, 500]);
      });
