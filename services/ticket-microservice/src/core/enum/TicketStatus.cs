namespace core.enums;

/// <summary>
/// Ticket status (per lab2-template v1 spec: PAID, CANCELED)
/// </summary>
public enum TicketStatus
{
    /// <summary>
    /// Ticket is paid
    /// </summary>
    Paid = 0,

    /// <summary>
    /// Ticket is canceled
    /// </summary>
    Canceled = 1
}
